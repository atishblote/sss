import { TimeStatusPipe } from './time-status.pipe';

describe('TimeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
